# API Directory Deprecated

The local FastAPI machine learning backend has been fully migrated and deployed to Hugging Face Spaces. 
This means you no longer need to run the `.py` scripts locally using `uvicorn`.

**Deployed API:**
`https://giridharanks-derma-ai-api.hf.space`

Please verify `.env.local` to see the updated `ML_API_URL` environment variable. The scripts here are kept only for reference/history and have been explicitly disabled.
